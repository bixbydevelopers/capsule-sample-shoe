capsule {
  id (example.shoe)
  version (2.1.2)
  format(3)
  targets {
    target (bixby-mobile-en-US)
  }
  capsule-imports {
    import (viv.core) { as (core) }
    import (viv.common) { 
      as (common) version (3.28.9)
    }
    import (viv.money) { 
      as (money) version (2.18.9)
    }
    import (viv.entity) { 
      as (entity) version (3.14.9)
    }
  }
}